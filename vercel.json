{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "echo '--- Starting Build Command (@vercel/python) ---' && python3.12 -m pip install -r requirements.txt && python3.12 -m flask --app api.index:app init-db && echo '--- Finished Build Command (@vercel/python) ---'"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.py" }
  ]
} 